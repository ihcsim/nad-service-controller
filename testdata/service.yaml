apiVersion: v1
kind: Service
metadata:
  name: test-svc
  annotations:
    test.dev/networks: ""
spec:
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
    - name: https
      port: 443
